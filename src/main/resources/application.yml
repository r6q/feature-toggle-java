db:
  host: localhost:3316

spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    password: feat_p
    username: feat_u
    url: jdbc:mariadb://${db.host}:3316/feat
  jackson:
    default-property-inclusion: non_absent
  flyway:
    password: feat_p
    user: feat_u
    url: jdbc:mariadb://${db.host}:3316/feat
